export enum LoginPageTexts {
  errorMessageLockedOut = "Epic sadface: Sorry, this user has been locked out.",
}

export enum LoginPageTexts {
  wrongPasswordOrUser = "Epic sadface: Username and password do not match any user in this service",
}
